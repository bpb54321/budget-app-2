import React from "react";

const TransactionListFooter = () => {
  console.log("TransactionListFooter");
  return <p>This is the transaction list.</p>;
};

export default React.memo(TransactionListFooter);
